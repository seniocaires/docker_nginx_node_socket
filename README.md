# Aplicacao com Docker, NGINX, Nodejs e Socket

```shell
git clone https://github.com/seniocaires/docker_nginx_node_socket.git
cd docker_nginx_node_socket
git clone https://github.com/socketio/socket.io.git
docker-compose up
```
